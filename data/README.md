# Data Layout

- `raw/` keeps external inputs that should not be committed.
- `processed/` keeps derived datasets that can be regenerated.
- `synthetic/` keeps generated phase datasets and task instances.
- `manifests/` keeps lightweight metadata that points at concrete dataset versions.
